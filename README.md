### Environment Installation

1. run inside pymssp: pipenv shell
   
2. run inside pymssp: pipenv install
   
3. Restart the IDE. Otherwise the virtual environment might not show up inside the notebook.
   
4. Choose the virtual env when prompted for a kernel inside the notebook.

![Project Screenshot](env.png)

